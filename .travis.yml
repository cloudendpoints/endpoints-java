language: java
jdk:
  - openjdk9
install:
 - JAVA_HOME=$(jdk_switcher home openjdk9) ./gradlew classes testClasses
after_success:
  - ./gradlew jacocoTestReport
  - bash <(curl -s https://codecov.io/bash)
